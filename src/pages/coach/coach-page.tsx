import { ScreenStub } from "@/components/forge/screen-stub";

export function CoachPage() {
  return (
    <ScreenStub
      title="Coach"
      description="A running feed of every recommendation. Coming in a later phase."
    />
  );
}
